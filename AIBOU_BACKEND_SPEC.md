# AIBOU バックエンド初期版仕様

作成日: 2026-09-12

状態: 2026-09-12に推奨方針と実装着手を承認済み。初期実装は `monitor/`。取得範囲と制約は `monitor/README.md`、検証結果は `monitor/QA/VERIFICATION.md` を参照。

## 1. 目的と今回の範囲

Macの内部情報を収集・整理し、11領域のタブと数値表で確認できるネイティブアプリを作る。収集処理と表示を分離し、後から相棒・部屋のUIへ接続できる構成にする。

今回の対象は監視・閲覧・集計。キャラクター、2D/3D表現、ゲームエンジン、AI会話、マネタイズ、自動修復、ファイル削除、プロセス終了操作は対象外。

既存の `pixel-house/Telemetry.swift` 等にはCPU・メモリ・プロセスの実測処理がある。利用できる部分を活用するが、今回合意したCPUの分母やメモリの定義に合わせる。既存の体験試作と、今回の数値検証画面を区別する。

以前の `AIBOU_REQUIREMENTS.md` と今回の範囲が異なる箇所では、この対話の新しい合意を優先する。

## 2. 確定した運用条件

| 項目 | 合意内容 |
|---|---|
| 配布 | Macへの直接配布。Mac App Store対応を初期版の条件にしない |
| 権限 | 通常権限の基本監視と、ユーザーが任意で有効化する追加収集を分離 |
| 追加アクセス | 必要に応じてフルディスクアクセス・管理者認証を利用。両者を同一視せず、保護の回避や全情報取得を保証しない |
| 検証対象 | まず手元のApple Silicon Mac。別機種・Intel・別macOSバージョンへの保証は検証後に拡張 |
| 更新 | 基本監視は約2秒間隔。詳細調査は必要時に切り替える |
| 重い処理 | ディスク走査等は基本監視とは別処理にし、停止・再開できる |
| 起動・終了 | 起動で観測開始。一時停止・再開を画面に用意。数値画面を閉じるとアプリも終了し観測停止 |
| 常駐 | ログイン時自動起動・独立した常駐サービスは初期版の対象外 |
| 表示 | 11領域すべてにタブを用意。取得可能な項目は実測、条件付き項目は追加収集で対応を試す |

追加権限を利用する設計への同意は、この調査セッションから直ちにOS設定を変更したり、常駐の管理者プロセスを導入したりする指示ではない。完成アプリの有効化操作と認証の経路を実装する。

## 3. 指標の共通契約

各指標は、値だけでなく、単位、取得元、計測日時、計測区間、算出方法、取得状態を持つ。

- 実測値、カウンタ差分からの算出値、推定を区別する。
- 未計測、初回差分待ち、権限不足、機種非対応、取得失敗、部分取得、停止中・過去値を区別する。
- 未実装と機種非対応を区別する。未実装のまま完成済みとは扱わない。
- 欠損を0で埋めない。古い値を現在値として扱わない。
- プロセスはPIDだけでなく起動時刻を含めて識別し、PIDの再利用を処理する。
- 速度は実際の経過時間に対するカウンタ差分で計算し、カウンタのリセットやスリープ・再開を処理する。
- プロセス・ネットワーク・デバイスの列挙は観測時点の情報であり、短命な対象の完全捕捉は保証しない。
- ホスト全体、物理デバイス、論理インターフェース、プロセスに計上された値を混同しない。

## 4. 11領域の取得・表示仕様

| タブ | 初期版で扱う情報 | 定義・制約 |
|---|---|---|
| ストレージ | ファイル階層、ファイルとフォルダの論理サイズ、取得できる割当済みサイズ、分類候補、アプリ関連フォルダ、プロセス別I/O、ディスクI/O速度 | サイズと削除で解放できる容量を区別。プロセスI/Oとデバイス全体のI/Oを区別。取得元が読み書きを分離しない場合は、その合計値を読み書き別の値に見せない |
| CPU | 論理コア数、各論理コアの稼働率、プロセス別CPU・現在のスレッド数・起動時刻・親PID・アプリ帰属、システム/ユーザー/アイドル、プロセス数・スレッド数 | 「コアごとの処理スレッド数」は稼働率へ置換。プロセスCPUは1コアを使い切ると100%、複数コアでは100%超。全体・各コアの稼働率とは分母を区別 |
| GPU | 取得できるGPU指標と、対応機種でのプロセス別GPU実行時間・帰属 | 実行時間を根拠なくGPU占有率へ変換しない。権限・機種による非対応を明示 |
| システムクロック | 対応機種でのCPU等の動作周波数、時計の変化、計測タイマーの遅れ | 3種類を別の指標にする。平均周波数を瞬時周波数と呼ばない。タイマーの遅れだけでクロック回路の故障と判定しない |
| メモリ | 物理RAM、スワップの総量・使用量、全体の取得可能なVM内訳、プロセス別の常駐量・フットプリント等、増加傾向タグ | ワイヤードはRAM内の領域としてスワップと分離。圧縮後の物理量と圧縮前相当量を区別。全プロセスの完全な内訳やRAM帯域・レイテンシが取得できない場合、理由を表示し別指標で代用しない |
| ファン・温度 | 対応機種でのファン最小/最大/現在RPM、CPU/GPU温度、OSの熱状態 | ファン非搭載と取得失敗を区別。OSの熱状態区分を°CやRPMに変換しない |
| バッテリー | 残量比、充電状態、取得できる満充電容量の絶対値、電源情報、直近の推移 | 容量の単位を確認し、%をmAhと扱わない。アダプタ定格、電池側の電圧・電流、算出電力を区別。「電力密度」「VRM温度」が取れない場合は理由を表示し、それらの値で代用しない |
| ネットワークI/O | インターフェース別・全体の送受信量/パケット数/速度、取得可能なプロセス別通信量・接続先・TCPのRTTや再送等 | VPN・仮想IF・loopback等の重複計上を避ける集計規則を明記。TCP RTTを全プロトコル共通の遅延と呼ばず、再送をアプリの失敗と断定しない |
| ディスプレイ | 接続ディスプレイ、取得できるEDID、現在の画素数・設定リフレッシュレート | 設定Hz、自アプリの描画/コールバック頻度、画面全体の実表示FPSを区別。実表示FPSが取れない場合はHzで代用しない |
| 外部接続I/O | 検出できる外部デバイス、接続・切断、名称・接続方式等 | バス・ドライバ・権限が公開する範囲を示す。未取得の識別情報を推測で埋めない |
| ハードウェア詳細 | Macの機種情報、CPU・RAM・ストレージ・GPU・ディスプレイ等について取得できる規格・型番 | 実機から取得できる情報を表示し、非公開の部品型番等は不明とする |

プロセスの親PIDと、利用者向けのアプリ/システムへの帰属は別の情報として保持する。親PIDだけでアプリ所有関係を断定しない。

## 5. ストレージ閲覧と分類

- 初期対象は内蔵の起動ボリュームでアクセスできる範囲全体。Library、`/opt`、ドットファイル、アプリの内部も閲覧対象とする。
- 外付け・ネットワークドライブは、ユーザーが対象に追加した場合だけ走査する。
- 階層を展開して閲覧し、子孫の論理サイズ合計をフォルダサイズとして主表示する。
- 取得できる割当済みサイズは補助表示とする。APFSの共有領域・スナップショット等により、サイズの合計と物理占有量・解放可能量は一致しない場合がある。
- リンクや重複したボリューム経路による循環・二重走査を防ぎ、集計規則を明記する。
- 計測時刻、未集計、途中結果、読み取り失敗をフォルダに表示する。アクセスできなかったフォルダを空フォルダとみなさない。
- 初期版は階層閲覧と再スキャンによる現在状態への更新。ファイル変更履歴の継続記録は対象外。
- 「関連フォルダを表示」から、アプリのキャッシュ・サポートデータ等を一覧できるようにする。

分類と診断のタグは根拠を開けるものにする。

| タグ | 根拠と表示 |
|---|---|
| システムデータ候補 | 保存場所や用途によるAIBOUの分類。macOS設定の分類・総量との完全一致は保証しない |
| アプリに関連 | bundle情報等で関連を確認できるものと、名称・標準保存先などから推定したものを区別 |
| メモリ増加が継続 | 観測期間、増加量、対象プロセスを示す。メモリ増加だけで高確率のリークと判定しない |

## 6. 履歴と保存

| データ | 保存方針 |
|---|---|
| 全体の負荷・バッテリー等の集計値 | 端末内に直近24時間。保存停止・削除を用意 |
| ファイル一覧・フォルダ集計 | 最新のスキャン結果だけ端末内に保存。計測日時を表示 |
| プロセス別の負荷推移 | 起動中のみ、保持量に上限を設ける |
| プロセスの接続先 | 起動中のみ。終了後は残さない |

観測停止・アプリ終了・スリープ等による欠測区間を実測値で埋めない。保存済みのスキャン結果は現在値と区別する。保存失敗や途中で終了した走査を完全な結果として読み込まない。

## 7. 実装構成と取得手段

実装方針はSwiftによる独立した収集・集計層とSwiftUIの数値検証画面。今回ゲームエンジンを導入する必要はない。新しい外部パッケージを追加せず、既存コードとOSの機能を活用する。

基本収集の候補はMach host統計、sysctl、Foundation、IOKit、CoreGraphics、プロセス情報を取得するlibproc。libprocはApple上流ヘッダにprivate interfaceと変更可能性の記載があり、安定した公開契約と同一視せず、対象OSで検証する。

追加収集ではOS標準コマンドも検討する。

- `nettop`: プロセス別通信量・接続先・TCP統計の候補。公開アプリAPIではなく、実行権限とOS版に依存する。
- `powermetrics`: 対応機種での周波数やGPU実行時間等の候補。管理者権限と機種対応を確認する。
- デバイスI/O・センサーの追加取得は、公開API、IORegistry、OS標準コマンド等の取得可否を実機で検証する。

現在のエージェント実行環境では一部コマンドが権限制限で失敗した。これは配布アプリの実行条件での非対応を証明しない。逆に、APIやコマンドに項目が存在するだけで取得成功を保証しない。

追加収集が有効でも、未対応のGPU値・RAM帯域/レイテンシ・VRM温度などを必ず取得できるとは約束しない。

## 8. 完成条件と検証

1. 11タブが開き、基本収集の実測値と測定条件を確認できる。
2. 条件付き取得について、対象Macで実際に試した結果と制約を記録する。未実装を非対応に偽装しない。
3. CPU・I/Oの差分計算、PID再利用、初回値、欠損、停止・再開を検証する。
4. ファイルツリー・フォルダ合計について、隠しファイル、深い階層、リンク、アクセス失敗、途中停止を検証する。
5. 24時間の保持上限、最新スキャンの保存・復元、詳細履歴の非永続化を検証する。
6. 機器非搭載・権限拒否・外部デバイスの接続変化・コマンド失敗が基本監視を停止させないことを確認する。
7. 数値表示、タブ操作、一時停止・再開、終了での観測停止を確認する。
8. 通常監視と全ディスク走査の負荷を実測する。性能目標の達成を実測なしに断定しない。
9. 変更に適したテスト・コンパイル確認・実機確認を行い、未検証環境と残る制約を報告する。

更新間隔の詳細モード初期値、履歴の間引き、走査の並列度、メモリ増加タグの具体的な閾値は実装・測定で調整する技術パラメータ。上記の値の意味、保存範囲、権限、完成条件を変更するものではない。

## 9. 根拠資料

- CPU計測: https://github.com/apple-oss-distributions/xnu/blob/main/doc/observability/recount.md
- VM統計: https://github.com/apple-oss-distributions/xnu/blob/main/osfmk/mach/vm_statistics.h
- libprocの契約: https://github.com/apple-oss-distributions/xnu/blob/main/libsyscall/wrappers/libproc/libproc.h
- システムデータの説明: https://support.apple.com/guide/mac-help/find-and-delete-files-on-your-mac-syspf5a64aa6/mac
- メモリの意味: https://support.apple.com/guide/activity-monitor/view-memory-usage-actmntr1004/mac
- ファイルの割当済みサイズ: https://developer.apple.com/documentation/foundation/urlresourcekey/totalfileallocatedsizekey
- App Sandbox: https://developer.apple.com/documentation/security/accessing-files-from-the-macos-app-sandbox
- 熱状態: https://developer.apple.com/documentation/foundation/processinfo/thermalstate-swift.property
- GPU・周波数、通信のコマンド仕様: 調査対象macOSに同梱の `powermetrics(1)`、`nettop(1)`、`iostat(8)` マニュアル

上記は取得経路と意味の根拠であり、この初期版の実装成功や全機種対応の証拠ではない。
