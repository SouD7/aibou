# 電気テレポート検証 — 2026-09-14

- `./test.sh`: 全11スイート成功。追加スイートは5姿勢の全20方向、消失後の放電・移動中の非表示・到着放電後の出現、連打の最終選択、終了時の単一表示、停止・モーション低減・焦点表示による中断を検証。
- `./run.sh --build`: Swift 5 / macOS 13 / arm64、warnings-as-errorsでビルド成功、アドホック署名成功。
- 実SpriteKit描画で立つ→眠る、眠る→読むを10fpsで取得。元位置・経路・到着点の電気エフェクトと、アバターの非表示区間・到着後の姿勢を確認。
- ネイティブUIでも「眠る」「読む」を押し、移動中の雷と、ベッド・本棚での再出現を確認。背景アニメーションは継続。
- `standing-to-sleeping.webp` / `sleeping-to-reading.webp`: 実描画PNGを1003×565に縮小した動作見本。最後のフレームを900ms保持。生PNGは再生成可能なため保存せず、時刻メタデータを保存。
- `phases.jpg`: 立つ→眠るの0.0 / 0.2 / 0.4 / 0.6秒。

通常の演出時間は0.84秒。立ち姿差分同士、キャラクターのみ表示、モーション低減、停止中は即時切り替え。連打時は進行中の移動を完了してから最後の選択へ向かうため、最大で約2回分の演出時間になる。
