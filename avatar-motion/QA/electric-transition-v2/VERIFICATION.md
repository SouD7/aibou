# 電気テレポート v2 検証

- 火花0.1秒、経路全体の直線光0.1秒、即時出現＋横ずれノイズ0.3秒。合計0.5秒。
- 色：薄い水色 #B8EAFF、白 #F0FBFF。移動する光球、曲線の軌跡、紫の枝、出現前の放電を削除。
- `./test.sh`: 全11スイート成功。位相境界0.1/0.2/0.5秒、固定された直線全体のパス、全20方向、到着後のノイズ表示と解除、一時停止中のノイズ解除、連打の最終姿勢、動きを抑える設定、キャラクターのみ表示を確認。
- `./run.sh --build`: warnings-as-errorsでビルド成功、署名成功。
- 読む→立つを30fpsのSpriteKit実描画で取得。火花・直線光・横スライスの干渉・終了後の通常表示を目視確認。動作見本はreading-to-standing.webp（冒頭700ms、末尾900ms保持）。phases.jpgは0.03/0.13/0.27/0.60秒。
- ネイティブUIでも「眠る」を押し、ベッド上の横ノイズと復帰を確認。
- 原画と背景素材の変更なし。出現ノイズは共有サブテクスチャを初回に作成して再利用し、終了時に隠す。通常モーションの格子変形はノイズ終了後に戻る。
- 生PNGは再生成可能なため削除し、時刻メタデータと圧縮見本を保存。撮影手順はREADME参照。
