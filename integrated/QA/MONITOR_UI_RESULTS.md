# 統合版の詳細パネル配色検証

2026-09-15 / `codex/integrated-monitor-ui`

## 変更

- コンポーネント選択ポップと同じ青黒の背景、ティールの操作色、白い細枠、角丸、影を詳細・メンテナンス・アプリに適用。
- カードとグループ枠、各画面内の詳細シートにも配色を継承。
- 共通配色は `monitor/Sources/MonitorAppearance.swift`。既定値はシステム配色で、元Monitorと統合版の全機能モニターは対象外。
- 統合版の配布用アプリを再ビルド。

## 検証結果

- `./integrated/run.sh --build`: 成功（警告はエラー扱い）。
- `./integrated/test.sh`: 全4スイート成功。
- `./monitor/test.sh`: ALL TESTS PASSED。
- Monitor全ソースのコンパイラ型チェック: 成功。
- `git diff --check`: 成功。独立コードレビュー: 指摘なし。
- macOS実画面: 基準の小ポップ、ストレージ詳細、メンテナンス、アプリ一覧の背景・文字・カード・操作色を目視確認。
- メンテナンスの原因候補詳細とアプリ詳細にも同じ配色が表示されることを確認。
- アプリ名検索と検索解除、詳細の開閉、Escapeによる詳細シートの閉じる操作を確認。

視覚確認は現在のMacの全画面表示で実施。全ハードウェアタブや全ウィンドウサイズの組合せを網羅した画像検証は未実施。

## 水色の回路流光（同日追記）

- `MonitorCircuitFlow.swift` で6経路の背景アニメーションを追加。配色はアバター遷移と同じ水色と青白い芯。
- 全18区間が水平または垂直で、斜めの区間がないことを座標検査で確認。
- 最新の統合版ビルド（警告はエラー扱い）、Monitor全ソース型チェック、差分チェック、独立レビューが成功。
- ネットワーク詳細・メンテナンス・アプリ一覧を実アプリで表示し、背景の光と文字・カードの見え方を確認。時間を変えた詳細画面のスクリーンショットで光の移動と直角の曲がりを確認。
- Escapeで閉じる操作を確認。装飾はヒットテストとアクセシビリティ対象から除外。
- モーション低減・非アクティブ時の停止はコードレビューで確認。OS設定を切り替えた実機検証や負荷の実測は未実施。

## 文字・カード遮蔽と速度変更（同日追記）

- カードを完全不透明に変更。文字・操作欄・見出し・表の範囲をアンカーで取得し、背景描画から除外。重なる除外範囲も再表示されない累積クリップを使用。
- 移動速度を110から220pt/秒へ変更。休止間隔は1.8秒を維持。
- 最新ビルド、Monitor既存テスト全件、全ソース型チェック、独立レビュー、差分チェックに成功。
- 実画面でネットワーク詳細・メンテナンス・アプリ一覧を確認。文字領域に光が重ならず、カード内の光が完全に隠れることを確認。
- メンテナンスをスクロールしてカードの遮蔽を確認。各画面のEscapeによる閉じる操作も確認。
