# Design

## Source of truth

- Status: Active（固定配置の2D画面素材 v3。アプリ実装は後続）
- 採用版: 2026-09-14のユーザー指定でv3へ戻す。v4・v5は履歴として保持し、現行素材・配置・アバター基準座標の参照には使用しない。
- Last refreshed: 2026-09-14
- Primary product surfaces: AIBOUの部屋画面。黒／白テーマそれぞれに、俯瞰と水平の2固定カメラを用意する。
- Evidence reviewed: [部屋デザイン v2](design/room-concepts/v2/README.md)、[家具コンポーネント v1](design/room-components/v1/README.md)、[2D制作要件](design/room-2d/v3/production-brief.json)、[配置・領域の正本](design/room-2d/v3/manifest.json)、[配置改訂](design/room-2d/v3/cpu-revision.json)、[Monitor概要](monitor/README.md)、[計測値の扱い](monitor/SOURCES.md)。[旧Pixel House案](design/pixel-house/BRIEF.md)の住宅・ピクセルアート方向は現行デザインへ適用しない。

## Brand

- Personality: 親しみやすい相棒が暮らす、清潔で精密な電脳空間。近未来的だが荒廃・威圧・過剰な軍事感を避ける。
- Trust signals: PC機能と家具の対応が一貫し、状態が視覚と文言の両方で判別できること。実測値、推定値、欠損を混同しないこと。
- Avoid: 住宅風ピクセルアート、汚れたディストピア、過密なHUD、読めない装飾文字、強すぎる常時点滅、取得不能値を0として見せる表現。

## Product goals

- Goals: 白黒2テーマ、俯瞰／水平2視点の計4画面について、背景と固定家具を同一構図へ統合しつつ、家具ごとに独立表示・クリック判定できる2Dレイヤー素材を作る。各コンポーネントの視認性を優先する。中央の立ち姿スペース確保を必須とせず、ベッドの寝姿・椅子の座り姿など状態に応じてアバターを配置する。
- Non-goals: 今回はネイティブアプリ実装、バックエンド接続、新しいアバター素材の制作、自由な視点移動、3Dモデル制作を行わない。
- Success signals: 4画面が同じ部屋・同じ家具配置として読める。全家具が背景から分離され、透過境界と重なり順が破綻せず、見た目と同じ座標系で各クリック領域を再現できる。

## Personas and jobs

- Primary personas: PCの状態をアバターの生活空間として眺めたいmacOSユーザー、および素材を後続のネイティブ画面へ組み込む開発者。
- User jobs: 俯瞰／水平と黒／白を切り替える。家具を選び、対応するPC機能の詳細へ進む。アバターの状態を部屋の中で認識する。
- Key contexts of use: 常駐または繰り返し開くデスクトップ画面。情報確認と雰囲気を楽しむ用途が同居し、長時間表示しても刺激が強すぎないことを優先する。

## Information architecture

- Primary navigation: テーマ切替（黒／白）、固定カメラ切替（俯瞰／水平）、家具選択。
- Core routes/screens: 1つの部屋画面を4バリアントで提供する。素材検品では [素材ビューア](design/room-2d/v3/index.html) でレイヤー表示と完成見本を切り替える。
- Content hierarchy: 背景 → 固定家具 → 将来のアバター状態 → 選択・フォーカス表示 → 詳細情報。部屋内モニタのバックエンド情報は後続実装で差し替える面として扱う。

## Design principles

- Principle 1: 4画面を別々の絵ではなく、同じ正方形の部屋と固定配置を異なるテーマ・視座で見せたものとして揃える。
- Principle 2: 見た目は一体の部屋でも、家具、可動部、発光部、モニタ面、クリック領域を独立して制御できる構造にする。
- Tradeoffs: 固定カメラと固定配置に限定して2D表現の品質と軽さを優先する。家具の位置関係、寸法感、造形の同一性を両視点で揃えたうえで、明瞭なシルエットとクリックしやすさを保つ。現行の配置・寸法感・描画順はv3の素材とmanifestを正本とする。

## Visual language

- Color: 黒はグラファイト／チャコール、白はセラミックホワイト／淡いグレー。両テーマともシアン・アイスブルーを主光、ラベンダーを副光とし、マゼンタは控えめに使う。
- Typography: 今回の画面素材には説明文字を焼き込まない。後続UIはmacOS標準に近い可読性の高いサンセリフを使う。
- Spacing/layout rhythm: 1672 × 941 pxを基準キャンバスとする。中央に固定の空き領域は設けず、家具の輪郭と動作の見やすさを優先する。CPU/GPUは右壁の手前側に接する大きめの筐体とし、内向きの側面に脳の刻印を入れる。
- Shape/radius/elevation: 面取りした金属パネル、隠し配線、ボルト、細い回路線、抑えた接地影。家具は人が使える寸法感と明確な輪郭を持たせる。
- Motion: CPU/GPUの球、ファン、時計、通信パルス、充電表現などは後続アニメーション用に分離可能とする。常時一斉点滅は避ける。
- Imagery/iconography: ベッド＝バッテリー、本棚＝ストレージ、デスクとチェア＝メモリ、豆電球状の配列＝CPU/GPU、電波塔＝ネットワークI/O、時計＝システムクロック、ファン2台＝冷却、モニタと壁へのケーブル＝ディスプレイコントローラ、外から床へ刺さる太いケーブル＝外部接続。

## Components

- Existing components to reuse: [部屋 v2](design/room-concepts/v2/README.md)の白黒外装と発光回路、[家具 v1](design/room-components/v1/README.md)の9種の造形・メタファー。これらは構図とスタイルの資料であり、そのまま最終スプライトとはみなさない。
- New/changed components: 各テーマ・各視点の背景レイヤー、ベッド、本棚、デスク、チェア、CPU/GPU、電波塔、時計、ファン1、ファン2、モニタ＋壁ケーブル、外部接続ケーブルの透過レイヤー。モニタ表示面、可動部、発光部は後続差し替え用の分離候補とする。
- Variants and states: `black|white` × `overview|horizontal`。アバターは今回制作せず、将来の寝姿・座り姿などをポーズ別の位置へ後置きする。家具の点滅・回転連番と表示面・時計部品を納品し、ランタイムと実測値連携は後続とする。
- Token/component ownership: 成果物は `design/room-2d/v3/` 配下に保存し、`manifest.json` をレイヤーファイル、描画順、クリック領域、アバターのポーズ別配置先の契約とする。`index.html` は検品用、`scenes/` は透過素材、`sources/` は生成・合成元を保持する。画像・参照・座標・再合成の検証結果は `validation.json` に記録する。

### 固定レイアウトと描画順

- 部屋配置: 西壁の南／手前寄りにベッド、西壁中央に本棚、北西／奥にデスクとチェア、西壁のデスク上に時計、北壁中央付近にモニタと壁接続ケーブル、その上にファン2台、北東／奥右に電波塔、右壁の手前側に脳の刻印を持つCPU/GPU、南東／手前右に外部接続ケーブルと床ソケットを置く。
- 基本描画順: 背景（床・壁）→ 壁付け／奥側家具 → 床置き家具 → 前景家具・ケーブル → 将来のアバター → 選択・フォーカス表示。実際の交差はmanifestの`z`で固定し、必要な前景遮蔽が判明した場合は家具の前景マスクを別レイヤーにする。
- アバターのポーズ別配置先: ベッドの寝姿と椅子の座り姿にそれぞれ仮の基準点を定義する。共通の立ち位置・占有枠は設けず、ポーズ素材に応じて基準点と家具の前景遮蔽を調整する。

## Accessibility

- Target standard: 後続ネイティブ実装ではmacOSの標準操作とWCAG 2.2 AA相当を目標とする。今回の素材はその達成を妨げない分離とコントラストを確保する。
- Keyboard/focus behavior: すべての家具クリック領域にキーボードフォーカスを割り当て、見えるフォーカス表示と一意の名称を提供する。小さい時計・ファン等は見た目より広い判定領域を許容する。
- Contrast/readability: 発光色だけで選択・状態を伝えず、輪郭、明暗、文言を併用する。モニタ上の数値は背景回路より高い可読性を持たせる。
- Screen-reader semantics: 家具名と対応機能、状態、取得不能理由を別要素として読み上げられる構造にする。欠損を数値0として読み上げない。
- Reduced motion and sensory considerations: モーション低減時は高速ランダム点滅を静止または低頻度の緩やかな密度変化へ置換し、ファン回転・通信パルス・充電走査も停止または大幅に減速する。強い全画面フラッシュを使わない。

## Responsive behavior

- Supported breakpoints/devices: 基準はmacOSの可変サイズウインドウ。素材キャンバスは常に1672:941の比率を保つ。
- Layout adaptations: レイヤー画像、アバターのポーズ別配置先、クリック領域は同じ基準座標を共有し、表示サイズへ同一倍率で拡大縮小する。座標を個別に丸めず、上下左右に余白が出る場合は同じ原点オフセットを全要素へ適用する。
- Touch/hover differences: macOSではホバーで対象を予告し、クリックまたはキーボード操作で確定する。将来タッチ対応する場合も同じ論理領域を使い、最小判定寸法を別途確保する。

## Interaction states

- Loading: 素材または状態値の読込中は部屋を保持し、対象箇所に控えめな待機表示を出す。実測前の値を0で仮表示しない。
- Empty: アバター素材がない今回の画面ではベッド・椅子に仮の配置先を定義する。後続製品では空の理由を必要に応じて説明する。
- Error: 欠けたレイヤーや読込失敗を特定できる名称で表示し、他のレイヤーは検品可能なまま保つ。
- Success: 家具選択は輪郭と名称で応答し、選択中の対象を色だけに依存せず示す。
- Disabled: 利用不能な対象はクリック不可にし、非対応・権限不足・取得失敗・計測待ちなどの理由を保持する。
- Offline/slow network, if applicable: ネットワーク未接続と計測取得失敗を区別する。通信がない状態をエラーと決めつけない。

## Content voice

- Tone: 簡潔で落ち着いた日本語。キャラクター性は親しみを加えるために使い、計測事実を曖昧にしない。
- Terminology: 家具名と対応機能を併記する（例「ベッド（バッテリー）」）。`未対応`、`権限不足`、`取得失敗`、`計測待ち`を使い分ける。
- Microcopy rules: 取得不能値を`0`や正常状態として表現しない。実測、差分からの算出、OSによる推定を必要に応じて明示する。

## Implementation constraints

- Framework/styling system: 今回は画像生成とPythonによる透過・領域分離を使用できる。必要な外部ツール、ライブラリ、アプリケーションの導入も許可済み。ネイティブ実装方式は今回確定しない。
- Design-token constraints: 白黒で家具形状、固定配置、基準座標、クリック領域IDを共通化し、主発光色もシアン／アイスブルー＋ラベンダーに固定する。
- Performance constraints: 将来の高速点滅は実測更新周期と分離し、画面全体の再生成を避ける。レイヤー数と透過画像サイズは画質確認後に最適化する。
- Compatibility constraints: 2D固定カメラのみ。水平視点は暫定的に旧 `02-southeast`（南東から北西）を採用するが、ユーザーは方角をまだ指定していない。俯瞰と水平の家具配置・IDは対応させる。
- Test/screenshot expectations: 4画面を1672 × 941 pxで合成し、透過境界、欠損画素、白黒の配置対応、俯瞰／水平の位置関係、描画順、寝姿・座り姿を想定した家具の視認性を目視確認する。各クリック領域はリサイズ前後で見た目の対象と一致することを検証する。成果物は `design/room-2d/v3/{manifest.json,index.html,scenes,effects,previews,sources}`。家具PNGは固定配置用で、脚間や塔内側などに局所背景・陰影を含む。任意移動・背後歩行用の完全独立切り抜きではない。ブラウザ実動作の検品はURLポリシー制約により未実施。

## Open questions

- [ ] 水平カメラを旧 `02-southeast`（南東→北西）で確定するか / ユーザー / 水平素材の最終構図と家具の見え方
- [ ] 将来のアバター状態一覧、足元基準点、最大占有枠を確定する / デザイン・実装担当 / ポーズ別基準点と前景遮蔽レイヤー
- [ ] 家具の選択後に表示する詳細UIの形式を確定する / プロダクト担当 / フォーカス遷移、読み上げ、モニタとの役割分担
- [x] 2D画面素材 v3：右壁側の大きめのCPU/GPUと脳の刻印、ファンの羽分離、消灯配線と断続的な流光。RGBA、参照、再合成、連番の差分、ORA構成を検証。詳細は [制作README](design/room-2d/v3/README.md) と [検証結果](design/room-2d/v3/validation.json)。

### v3 配置・アニメーション方針

CPU/GPUは黒白・俯瞰水平とも右壁の手前側へ接する配置とし、大きめの筐体の内向き金属面に脳の刻印を入れる。ファンは固定筐体・回転羽・固定グリル・固定軸を別PNGとし、羽だけを回転する。部屋の配線は消灯ベースに、有限長のシアン・青・ラベンダーの光が断続的に流れる24フレームを重ねる。家具インジケーターとCPUの球は独立した発光部として扱う。

アバターは寝姿・座り姿などポーズ別に配置し、寝具や肘掛けなどの遮蔽はポーズ素材制作時に用意する。今回の配置先は仮の基準点で、アバターや遮蔽の実装完了を意味しない。

### 不採用版の履歴

2026-09-14、ユーザーがv4・v5よりv3を選択したため、参照先をv3へ戻した。以下は試作履歴であり、現行仕様には適用しない。

- [v4](design/room-2d/v4/README.md)：ベッドのシングル幅化、電波塔・ファン・デスクなどの視点間調整。
- [v5](design/room-2d/v5/README.md)：ベッドを広めにして長辺を左壁へ沿わせた配置。
