# R4 コード担当の独立レビュー

担当: `r4_code_review`。2026-09-13 22:14:37 JSTの固定コピーが対象。返却本文から親が要約・保存。

**COMMENT。CRITICAL 0、HIGH 0、MEDIUM 1、LOW 1。**

## 確定指摘

1. **MEDIUM: 機器一覧キャッシュの時刻。** DeviceSampler.swift:63–83、267–276。30秒以内の再利用時に行は元のrecordedAtを保持するが、一覧statusとpanel時刻は現在時刻になる。動的fixtureで取得1回なのに時刻が更新されることを確認。cache.dateを返し、一覧全体で取得時刻を維持する修正と回帰テストを推奨。確度は高。
2. **LOW: 保存索引の意味整合性。** StorageScanner.swift:246–262。JSONとgraphが正常なら、完了日時なし、件数・合計とnodesの矛盾を受理する。動的fixtureではroot 1個・12Bに対して999,999件・999,999Bの結果をsavedとして復元。通常生成物の不具合ではなく、所有者用ローカル保存物の破損時の防御不足。完了日時を検証し、派生値をnodesから確認・再計算することを推奨。確度は高。

再現コード・ログ・詳細は[統合レビュー](CODE_REVIEW_R4_2026-09-13.md)参照。

## 確認したその他の経路

- R3のObservation状態/区間、CPU tick失敗分類、直接process停止、保存graph、履歴allowlistの対応は維持されている。
- CommandRunnerの予約・取消・SIGTERM後の生存確認・timeout競合に新たな確定不具合なし。
- スキャン列挙終了後の集計中cancelがcompletedへ上書きされる窓はあるが、列挙済み内容の欠落を生じる経路ではないため新規指摘に数えない。
- 診断追加3ファイルと接続箇所も静的確認。秘密情報、任意コマンド注入、固定URL以外の外部送信は確認していない。

## 検証と制限

Sources 12、Tests 8、scripts 3、plist/privacy 2、公開文書4とR3記録を確認。親の開始時テスト/build/署名/lintと固定コピーの既存全テスト成功、2件の実行再現を照合した。固定コピーの最終buildは統合レビューに追記する。

診断追加機能は開発途中。専用テストがまだなく、持続時間・欠損・古さ・復旧・症状照合の動的検証は未実施。未完成という理由だけで指摘に数えない。専用LSP診断ツールはなく、Swift warnings-as-errorsコンパイルを代替証拠とする。

既知WATCHは管理者認証先の終了未検証、全件JSONのメモリ負荷、索引→保存登録の終了窓、MainActor履歴復元、将来の複数consumer。高重大度のセキュリティ、データ消失、クラッシュ経路は確認していない。
