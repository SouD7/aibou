# 再レビュー修正計画 — 2026-09-13

1. CoreSampler: デバイス別のディスク差分、プロセス列挙失敗の状態維持。集合変更と失敗注入を回帰テスト。
2. DeviceSampler: IORegistry entry identity、内蔵電池の厳密な選択、空集合・失敗・部分取得の分離。fixture検証。
3. Observation: 実際の計測区間をProcessSampleから引き渡し、直接・Store経由の出力を検証。
4. powermetrics: 件数・深さでの解析打ち切りを部分取得として公開。上限直前・到達後・深い構造を検証。
5. 統合: 不完全な機器一覧から偽の切断を作らない。停止の意味を画面と文書で明示。
6. 全回帰テスト・最適化ビルド・実機収集・大規模保存復元の合成負荷測定。独立コード/設計再レビュー、結果文書化。

既存アーカイブと別デモには触れない。外部依存は追加しない。ストレージ上限撤廃や複数アバターconsumerの導入はこの修正に含めない。
