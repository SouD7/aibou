# アプリ詳細のレビュー

2026-09-14。独立レビュー担当 `/root/r6_code_review` の最終報告を親エージェントが記録。

**APPROVE**。CRITICAL / HIGH / MEDIUM / LOWすべて0。

対象: ApplicationUsage.swift、ApplicationDetailView.swift、MonitorStoreの通信計測部分、ランチャーへの接続、AdditionalCollectorsの差分区間判定と関連テスト。

確認事項:

- 選択したインストール先と、その内包ヘルパー・子プロセスを照合する。同じIDの別インストール、別GUIアプリ、古い親PIDへの誤結合を除く。
- nettopはプロセス行だけを合算し、接続行・重複PID行・初回累積値を加算しない。計測前後にPIDの起動時刻を確認する。
- 読取不可、権限不足、未計測の状態を保持し、値がある場合だけ経過時間により過去値とする。
- 詳細画面を閉じたときは、その画面が開始した通信計測だけをキャンセルする。

レビュー中に、共有通信計測のキャンセル所有権と、欠測値の状態を過去値で上書きする問題を修正した。修正後の全Sourcesのwarnings-as-errors型チェックとdiff checkが独立担当側でも成功した。

容量処理は別実装担当 `/root/app_detail_storage` が実装・専用テストを行い、親エージェントが追加確認した。親の確認で見つかったシンボリックリンク後の兄弟フォルダの読み飛ばし、ルート間のエラー予算の累積、関連候補のパス逸脱を修正し、各回帰テストを追加した。独立コードレビュー担当による容量処理の確認はインターフェース境界に限る。

全体の実行結果と検証範囲はAPP_DETAIL_RESULTS.mdを参照。
