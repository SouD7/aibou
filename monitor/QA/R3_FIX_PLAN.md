# R3レビュー修正計画

対象: `CODE_REVIEW_R3_2026-09-13.md`。既存の計測値・保存済みユーザーデータを維持する。

1. Observationに基本監視の状態、監視区間と値の取得区間を追加する。停止中に完了した追加計測は元の計測時刻とstatusを維持する。
2. CPU tick取得関数を注入できるようにし、初回・取得失敗・回復をテストで区別する。
3. 保存ストレージの唯一のroot、path/ID一致、全nodeのroot到達性を反復処理で検証する。cycle・孤立node・深いtreeをテストする。
4. 履歴復元にも既存の許可リストを適用する。
5. 管理者計測の停止は直接processと昇格した子processを区別する。未検証の子process停止を保証せず、通常権限で検証できる取消経路と説明を改善する。

回帰テストを先に追加し、全テスト、最適化ビルド、署名・plist検証後、既存のコード/設計レビュー担当へ再確認を依頼する。起動中のアプリとユーザーの保存索引は操作しない。
