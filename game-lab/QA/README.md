# 検証記録

今回の単独パッケージ化の確認結果は [STANDALONE-VERIFICATION.md](STANDALONE-VERIFICATION.md) を参照してください。

それ以外の既存文書とログは、切り出し前の開発履歴です。ラボ統合版の記述や、当時の公開状態・ファイル配置が含まれます。大きな生成物（`captures/`、`results/`、`profiles/`）はGitとソースパッケージに含めていないため、過去の画像リンクはこのパッケージ内では開けません。元の作業フォルダとバックアップに残しています。

現在の代表画面は [20体験一覧](standalone-twenty-experiences.png) に同梱しています。描画画像一式とUIテスト結果は、各スクリプトの実行で再生成できます。
