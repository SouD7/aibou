# 展示館ホームの検証記録

2026-09-14。検証対象は展示館ホーム・20入口・既存回路ゲームへの接続。19個の未実装ゲーム本体の検証ではない。

## UIテスト

最終画像・レイアウト修正を含むビルドで、**展示館8ケース＋既存盤面回帰1ケースの計9つの異なるケースが合格**した。7ケースの一括実行は75.995秒・失敗0件。自由実験から「はじめから」を選ぶ追加ケースは単独実行で11.810秒・失敗0件。実ウィンドウを縮小する追加ケースは10.875秒・失敗0件。セットアップ失敗はなかった。

`UITests/GameLabUITests.swift`の`ExhibitionUITests`で検証する。テストごとに一時ディレクトリを作り、`--exhibition-save`と`--workshop-save`を別々に指定する。利用者の実際の保存データは読まない・変更しない。

最終実行では、下表の5ケースに加えて以下を確認した。

- マップをEscapeキーで閉じ、元の入口位置へ戻れる。
- 自由実験のOR課題を完成し、展示室へ戻ってから再開すると、自由実験のモード・課題・回路・ランプを保持する。案内付きアトリエにも戻れる。
- 既存の`testWorkbenchStaysFixedWhenShowingInstructionsAndResults`が合格。操作説明・結果の開閉および完成後も、ゲーム盤面と部品の位置が変わらず、プレイ画面にScrollViewがない。
- 自由実験を選んだまま展示室へ戻り、入口の「はじめから」を確認すると、案内付きアトリエが新しい盤面で開く。以前の自由実験画面に戻らず、未接続の出力とテスト不可の状態になる。
- ネイティブのウィンドウ右下をドラッグし、実寸を1400×893ptから1100×770ptへ縮小。5つのロビー入口と20個のマップ入口がすべて操作可能で、実ウィンドウ内に収まる。両画面にScrollViewはない。

最終実行の証拠：

- `QA/results/ui-20260914-195419-21565.xcresult`
- `QA/exhibition-final-ui-tests.log`
- `QA/captures/exhibition-final/`。`exhibition-room-a.png`〜`exhibition-room-e.png`、`exhibition-map-twenty-entrances.png`、`exhibition-empty-workbook.png`、`exhibition-play-resumed-after-relaunch.png`等の分かりやすい名前でも画像を保存した。画像の加工はしていない。
- 追加の開始し直し確認：`QA/results/ui-20260914-195904-22089.xcresult`、`QA/exhibition-restart-ui-test.log`、`QA/captures/exhibition-restart/`。
- 実ウィンドウ縮小確認：`QA/results/ui-20260914-200552-22476.xcresult`、`QA/exhibition-compact-ui-test.log`、`QA/captures/exhibition-compact/exhibition-compact-window-dimensions.txt`。実際のフレームは変更前`(56, 33, 1400, 893)`、変更後`(56, 33, 1100, 770)`。

コンパクト表示の目視証拠は`QA/captures/exhibition-compact/exhibition-compact-map.png`。20入口と閉じる操作が欠けずに表示されている。縮小直後に取ったロビー画像には共有デスクトップの別ウィンドウが写るキャプチャ上の乱れがあり、`exhibition-compact-lobby-compositor-transition.png`として区別した。このロビー画像は納品用にも正常描画の証拠にも使用しない。ロビー5入口の収まりと操作可否は、同じ実行のアクセシビリティ座標・isHittableで確認した。

納品向けの静止画には、**`QA/captures/exhibition-release/`の名前付きPNGを優先する**。0.35秒の遷移待ち後に取得した5室・マップ・作品帳・準備中入口のキャプチャである。従来の画像にはフェード途中の薄い文字が残るものがある。

静止画を取り直した実行`QA/results/ui-20260914-195807-21964.xcresult`では、3つの既存ケースが合格し、追加の開始し直しケースはテストのボタン選択で失敗した。macOSのTouch Barと確認ダイアログに同名の「最初から作る」があり、テストが両方を選択したためである。選択先を実際のシート内のボタンに限定し、上記の単独実行で合格した。アプリコードの追加修正や、成功状態を注入する処理は行っていない。

## 初回検証と修正の記録

| ケース | 初回実行 | フレーム安定待ち追加後 |
|---|---|---|
| 5室それぞれ4入口、マップに20個の一意な入口、室内にScrollViewなし | 合格 | 再実行不要 |
| マップを閉じると元の室・入口位置へ戻り、メモリ・ドックは準備中で開始不可 | 遷移途中のフレーム比較で失敗 | 合格 |
| B室からマップで接続ラボを選び、入口からD室へ戻る | 合格 | 再実行不要 |
| 初回作品帳は空、閉じると元のE室・入口位置へ戻る | 遷移途中のフレーム比較で失敗 | 合格 |
| A室から回路を開始、配線と入力を変更し、展示へ戻る／再開／アプリ再起動後も盤面保持 | 合格 | 再実行不要 |

初回は5件中3件合格。2件の失敗は、ボタンが操作可能になった直後のフレームを、SwiftUIの遷移が終わる前に比較していたもの。4秒以内に元の位置・寸法へ戻ることを待つ検証に変更し、同じアプリで2件とも合格した。許容差は0.5pt未満であり、大きく移動したままでも通る条件にはしていない。

合計5つの異なるケースが合格した。この段階ではB室の最終画像がまだアプリに入っていないため、初回キャプチャは最終の画像品質を示すものではない。

証拠：

- 初回：`QA/results/ui-20260914-194959-20952.xcresult`、`QA/exhibition-ui-tests.log`。43.46秒、3合格・2失敗。
- 位置復元の再検証：`QA/results/ui-20260914-195123-21130.xcresult`、`QA/exhibition-overlay-ui-tests.log`。12.66秒、2合格・0失敗。
- キャプチャ：`QA/captures/exhibition-initial/`、`QA/captures/exhibition-overlay/`。各`manifest.json`でテスト名と画像名を対応づける。

今回の2回には、共有デスクトップのSpaceによるセットアップ失敗はなかった。

## テスト互換性

展示館が単体アプリの初期画面になったため、既存`WorkshopUITests`に`--workshop`を明示した。自由実験は従来の`--sandbox`を維持。Windowメニューからのウィンドウ復帰は新しい「AIBOU · 展示館」と旧名の両方を扱う。

## 統合モニターでの実機確認

担当エージェントが実際の`AIBOUMonitor.app`を操作し、次を確認した。これらは上記の自動テスト件数には加算していない。

- CPUタブから展示館へ移り、モニターへ戻るとCPUの選択が保持される。展示館側でもA「回路としくみ」の所在を保持する。
- 作業中だったXORの回路を保持する。入力はA=0・B=0、Bと出力が接続済み、Aは未接続という状態が、展示館との往復後にも残る。
- 作品帳の完成済みAND回路1件を開き、作品内のA=1・B=1で出力1、Aを0にすると出力0になることを確認した。この操作は作品帳内だけに適用され、作業中の回路とは独立している。
- 作品帳の新しいノートを持つ女の子の切り抜きに、目立つ背景の残りや欠けがないことを確認した。
- 最新ビルドを再起動し、最後に開いていた総合ロビーへ復元されることを確認した。

統合版の画面記録：[総合ロビー](captures/exhibition-monitor/lobby.png)、[A「回路としくみ」](captures/exhibition-monitor/area-a.png)、[完成作品のある作品帳](captures/exhibition-monitor/workbook.png)。

1100×770ptのコンパクト表示は、上記の単体アプリXCTestによる実寸・操作検証を証拠とする。統合版でも同じ寸法で手動検証したという意味ではない。

## 最後の画面文言の調整

回路入口で重複していた質問を「つなぐ → 試す → 光り方をくらべる」という短い案内に変更し、単体版・統合版の両アプリを再ビルドした。操作、遷移、判定、保存の機能変更はない。上記の自動テストをこの文言だけの変更後に一式再実行したものではない。

続いて、ロビーのD扉の文字領域を看板内へ収め、扉名の縮小表示の下限を調整した。両アプリを再ビルドし、統合版を再起動した実画面で文字の収まりを確認した。最終のロビー画像はこの版である。両アプリのコード署名検証と納品ZIPの整合性検証は合格し、各ZIPに8枚の展示館素材が含まれることを確認した。最終ZIPのSHA-256は`output/exhibition-delivery/manifest.json`に記録した。

B室の最終画像と作品帳の新しい人物画像は最終7ケースの実行時に読み込まれ、キャプチャでも確認した。キャプション領域の高さを維持した版でマップ・作品帳の往復が合格している。

この記録は実行結果だけを述べる。企画上の学習効果、20ゲーム全体の完成、長時間の性能についての合格を意味しない。
