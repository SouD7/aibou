# AIBOU 窓辺の部屋 — Design QA

実施日：2026-09-09

final result: passed

## 比較対象と証拠

- Source visual truth：`mock/References/chill-with-you-official.jpg`。Chill with You: Lo-Fi Storyの公式Steamスクリーンショット、1920×1080。
- 実装用のオリジナル画像：`mock/Assets/lofi-room.png` と `mock/Assets/lofi-room-busy.png`、各1586×992。
- 実装：`mock/AIBOU.app`。CUAのネイティブウィンドウキャプチャで確認。
- Implementation screenshot path：ローカル保存なし。今回の会話内のCUA出力「公式の参考画面とAIBOUの実画面を比較」「修正後の長い台詞と参考画面を再比較」に参照画像と実装スクリーンショットを同じ比較入力として記録した。保存済みスクリーンショットがあるとは扱っていない。
- 表示画像の実寸：1181×768、JPEGヘッダーのSOFから確認。ネイティブSwiftUIでありCSS viewportとdeviceScaleFactorは該当しない。ウィンドウ設定は初期1260×850pt、最小1100×750pt。撮影時のポイント寸法・OS側の出力倍率は別途取得していない。
- 正規化：公式16:9と実装約1.54:1には縦横比差がある。画面クロームやピクセル単位の一致は評価対象外。中央右の人物、左の窓、机の前景、下部会話の各領域を相対位置で比較。実装の1586×992画像は縦横比を保ってcover表示し、引き伸ばさない。
- 状態：通常、CPU処理中、回復、観測停止、触れ合い、写真取得後。診断・思い出・設定パネルも開いて確認。

今回の依頼は雰囲気を寄せることであり、元ゲームの完全再現ではない。青空中心の昼景から夕景への変更、オリジナルキャラクター、CPU情報・思い出への入口はAIBOUとしての意図した差分。

## 比較履歴と修正

1. 初回の全体比較：参考画像と実画面を同じCUA出力に表示。共有デスク、人物の大きさ、窓の光、生活感、下部の控えめな会話表示を確認。常時サイドバーを除き、主画面を部屋中心に変更できている。
2. [P2・解消] 写真取得後、観測停止時の長い台詞が写真の明るい縁と重なっていた。該当領域を含む実画面で確認し、写真の位置を画像内比率 `(0.405, 0.79)` から `(0.19, 0.672)` へ変更。
3. 修正後：再ビルド・再起動。写真取得済み・観測停止の同じ状態、同じ1181×768の出力で再撮影し、公式画像と同じ比較入力に載せて確認。写真は会話領域の上へ移り、2行目の末尾まで読める。人物の顔と主操作にも重なっていない。

全景で会話・小物・ナビゲーションを読める解像度だったため、別ファイルへの拡大切り抜きは不要と判断。診断内容はパネルをスクロールした追加キャプチャで、処理別の数値、根拠、対処ボタンを重点確認した。画像を並べて合成したファイルやピクセル差分計測を作成したとは主張しない。

## 必須評価面

| 面 | 確認結果 |
| --- | --- |
| フォント・文字 | 日本語のシステムフォント、ロゴのみserif。22ptの台詞、12ptの操作、9〜11ptの補助表示に階層をつけた。長い台詞は2行で収まり、欠損時の文章も末尾まで表示。参照の欧文フォントとの同一性は求めない。 |
| 余白・配置 | 人物を中央右、状態を左上、操作を下部へ。顔を覆う大きなカードはない。詳細はスクロールパネルで、閉じる操作とデモ切替はスクロール外に残る。写真と台詞の重なりは解消。 |
| 色 | 夕暮れの暖色、セージの差し色、暗い半透明レイヤー、淡い会話文字。あんずへの変更で名前・主要ボタンなどのアクセントが変化することを確認。観測停止は彩度を落とすと同時に文と欠損記号で伝える。 |
| 画像 | 2D PNGを等方拡大し、顔や手を不自然に切らない。植物は実際にアルファのあるPNGで、背景の矩形はない。通常・集中の2ポーズを同じ画角で切り替える。参照の人物やゲーム画像はアプリに流用しない。 |
| 内容 | PCの数値は明確にサンプルと表示。診断には観測事実・関連候補・未計測事項が残り、取得できないときに既存値を実測として表示しない。未実装の音楽やAI会話の偽の操作を追加していない。 |

## 操作確認

- 通常18%/43%からCPU処理中86%/65%へ切り替え、台詞と集中ポーズを確認。
- 「今、重い？」で診断を開き、スクロールで根拠と対処案内へ到達。
- 「対処後の例を見る」でCPU86%→20%、メモリ65%→48%とデモである説明を確認。
- 人物クリック／なでるでハートと台詞が変化。3回目で写真と思い出が追加された。
- 写真をクリックして思い出パネルを開き、実際の触れ合い3回・4件の思い出を確認。
- 設定から差し色と動きを減らす設定を変更。Escapeで部屋へ戻れることを確認。
- 観測停止でCPU・メモリが「—」、デモ状態選択が無効、台詞が不明を説明。再開で戻る。
- 再起動後も小物・名前・思い出・変更した設定が保存されていた。
- 検証後は観測を再開し、差し色「若葉」・動きを減らす「オフ」へ戻して通常の部屋を表示。

## 残る範囲

未解消のP0/P1/P2なし。P3：本制作では写真を専用のフォトスタンド画像にすると、机上の遠近感をさらに揃えられる。大きな外部ディスプレイ、VoiceOverによる通し操作、実測の消費電力・常駐メモリは今回未検証。連続した呼吸・まばたき、Live2D、音楽再生はこのモックの範囲外。

## 完了チェック

- [x] 参考画面を開き、実装画像と同一入力で比較
- [x] 5つの評価面を確認
- [x] P2の写真・台詞の重なりを修正して再撮影
- [x] 主要操作、停止時、保存の動作確認
- [x] ネイティブビルド、モデル26項目、スクリプト構文、Info.plist検証
- [x] 通常の部屋を起動したまま引き渡し
